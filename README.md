# go2web
C# console application for implementing HTTP over TCP sockets. The project supports manual HTTP/HTTPS requests, human-readable response output, and web search through a command-line interface, without relying on built-in or third-party HTTP client libraries.
